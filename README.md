# 🏗️ andere 👷 flake 🚧

nh os switch .
nixos-rebuil switch

nh os switch .#HOSTNAME --target-host HOSTNAME

neue machine hinzfuegen

ssh-to-age < /etc/ssh/ssh_host_ed25519_key.pub

vim .sops.yaml -> den Key hinzufuegen

sops updatekeys secrets.yaml

sops edit ...
sops decrypt ...
